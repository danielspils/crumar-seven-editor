'use strict';

// probe.js — STUB
//
// Intended job: send controlled MIDI/SysEx to the Seven and record what comes
// back, so that byte meanings can be established by observation (Rule 2) rather
// than inferred. Every probe run should write its raw results into captures/.
//
// No protocol logic yet — no frames are sent or decoded until the scaffold
// stage is done and captures justify it.

module.exports = {};
