# Protocol (v1.37)

> **Stub.** No SysEx frame has been captured yet.

Per **Rule 2**, every fact on this page must trace to a specific file in
`captures/`. `captures/` is currently empty, so **the entire protocol is
`UNKNOWN`.**

## Frame structure

| Field        | Bytes | Meaning   | Evidence      |
|--------------|-------|-----------|---------------|
| _(all)_      | ?     | `UNKNOWN` | _no captures_ |

## Notes

- Transport: class-compliant USB-MIDI, SysEx. (Observed at the port level; frame
  internals are `UNKNOWN` until captured.)
- Do not populate this table from the manual or the web editor — captures only
  (Rules 1–3).
- Decoded interpretations here live **alongside** the raw hex in `captures/`,
  never as a replacement for it (Rule 5).
